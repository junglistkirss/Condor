# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET

on:
  workflow_dispatch: # Allow running the workflow manually from the GitHub UI
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x
      env:
        NUGET_AUTH_TOKEN: ${{ secrets.NUGET_TOKEN }}

    - name: Restore dependencies
      run: |
        dotnet nuget add source https://nuget.pkg.github.com/junglistkirss/index.json --name Private --username junglistkirss --password "${{ env.NUGET_AUTH_TOKEN }}"
        dotnet restore
    - name: Build
      run: dotnet build --configuration Release --no-restore
    # - name: Test
    #   run: dotnet test --no-build --verbosity normal

    - name: NuGet
      run: dotnet pack --no-build --configuration Release -o .

    - name: Push to GitHub package registry
      run: dotnet nuget push *.nupkg --api-key "${{ env.NUGET_AUTH_TOKEN }}" --source https://nuget.pkg.github.com/junglistkirss/index.json --skip-duplicate
